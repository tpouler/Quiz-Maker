import { render } from "@testing-library/react";
import Main from "../pages/index";

/**
 * This is a placehold test that is here only to make sure `npm test` will not choke because there are no tests. It should be removed as soon as you write a real test.
 */
describe("PLACEHOLDER", () => {
  test("main app render", () => {
    render(<Main />);
    //expect(screen.getByText("Quiz 1")).toBeVisible();
  });
});
